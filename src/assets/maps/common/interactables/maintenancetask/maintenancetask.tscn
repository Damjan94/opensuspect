[gd_scene load_steps=4 format=2]

[ext_resource path="res://assets/maps/common/interactables/standbutton/standbutton.tscn" type="PackedScene" id=1]
[ext_resource path="res://assets/common/classes/tasks-maitenance/base/base-task-maintenance.gd" type="Script" id=2]

[sub_resource type="GDScript" id=1]
script/source = "extends Timer


# How often to update when there are no peers connected to maintenance task
export(float) var wait_time_no_peers = 2.0
# How often to update when there are peers connected
export(float) var wait_time_peers = 0.1

func _ready():
	set_wait_time(wait_time_no_peers)

func set_has_peers(has_peers: bool):
	if has_peers:
		set_wait_time(wait_time_peers)
	else:
		set_wait_time(wait_time_no_peers)
"

[node name="MaintenanceTask" type="Node2D"]
script = ExtResource( 2 )

[node name="standbutton" parent="." instance=ExtResource( 1 )]
node_path = NodePath("..")

[node name="Timer" type="Timer" parent="."]
wait_time = 0.05
autostart = true
script = SubResource( 1 )
